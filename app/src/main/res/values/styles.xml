<resources>

	<!-- Base application theme. -->
	<style name="Mate" parent="Theme.AppCompat.Light.NoActionBar">
		<!-- Customize your theme here. -->
		<item name="colorPrimary">@color/primary</item>
		<item name="colorPrimaryDark">@color/primary_dark</item>
		<item name="colorAccent">@color/accent</item>
	</style>
	

	<style name="Mate.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

	<style name="Mate.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
